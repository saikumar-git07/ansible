- name: conditions
  hosts: localhost
  become: yes
  tasks:
  - name: check user exist or not
    ansible.builtin.command: id expense

  - name: say hello
    ansible.builtin.debug:
      msg: "hello"